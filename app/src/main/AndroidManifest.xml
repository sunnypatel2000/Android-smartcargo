<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smartcargo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartCargo">
        <activity android:name=".MajorViews.Ack"></activity>
        <activity android:name=".MajorViews.HomePage" />
        <activity android:name=".MajorViews.RegisterUser" />
        <activity android:name=".MajorViews.Details" />
        <activity android:name=".MajorViews.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MajorViews.AddNewOrder" />
        <activity android:name=".MajorViews.MainActivity" />
    </application>

</manifest>